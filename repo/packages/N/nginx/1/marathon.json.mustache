{ 
	"apiVersion": "v1", 
	"kind": "Pod", 
	"metadata": 
	{ 
		"name": "{{nginx.name}}", 
		"labels": 
		{ 
			"app": "nginx" 
		} 
	}, 
	"spec": 
	{ 
		"containers": 
		[ 
			{ 
				"name": "{{nginx.name}}", 
				"image": "{{resource.assets.container.docker.nginx-docker}}", 
				"ports": 
				[ 
					{ 
						"containerPort": 80 
					} 
				] 
			} 
		] 
	} 
}
